# React with Navigation API

This is a test of React manual routing with the new Navigation API

There are also references of using manual Routing with Hash URLs (AppHash.js) and with the History API (AppHistory.js)



